# dashboard — Project Dashboard (root)

Overview
- Top-level `dashboard/` contains public aggregator pages and consumer-facing dashboards used by organizers.
- See `layout.tsx` and `page.tsx` for entry points.

Notes
- This is separate from `app/dashboard/` (which is the Next.js app's route group).
- If you modify shared components used by both, update `components/` accordingly.

---
*Root-level dashboard module quick notes.*