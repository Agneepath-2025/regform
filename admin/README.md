# admin — Admin Utilities & Docs

Overview
- Top-level `admin/` contains admin UI docs and helpers.
- See `admin-docs/` for setup and operational guides (`ADMIN_PORTAL_SETUP.md`, etc.).

Guidance
- Use `admin-docs/` when configuring new admin accounts and deployment guides for portal-level changes.

---
*Admin documentation pointer.*